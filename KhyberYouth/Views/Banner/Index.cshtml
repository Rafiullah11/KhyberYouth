@model IEnumerable<KhyberYouth.Models.Banner>

<div class="container p-3 mt-lg-5">
    <div class="row ">
        <div class="col-9">
            <h3 class="text-warning">Banner List</h3>
        </div>
        <div class="col-3 text-end">
            <a asp-action="Create" asp-controller="Banner" class="btn theme-btn">
                <i class="bi bi-plus-circle"></i> &nbsp; Add Brand Logo
            </a>
        </div>
        <hr />
    </div>
    <div class="row border border-warning mt-3 p-2">
        <table class="table">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Description</th>
                    <th>Image</th>
                    <th class="text-center">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var banner in Model)
                {
                    var photoPath = "~/images/" + (banner.ImagePath ?? "default-image.png");

                    <tr>
                        <td>@banner.Name</td>
                        <td>The Brand picture</td>
                        <td>
                            <img asp-append-version="true" src="@photoPath" alt="@banner.Name" width="50" height="50" />
                        </td>
                        <td class="text-center">
                            <a asp-action="Edit" asp-route-id="@banner.Id">  <i class="fas fa-pen-to-square"></i></a> |
                            <a asp-action="Details" asp-route-id="@banner.Id"> <i class="fas fa-eye text-info"></i></a> |
                            <a asp-action="Delete" asp-route-id="@banner.Id"> <i class="fas fa-trash-alt text-danger"></i></a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
